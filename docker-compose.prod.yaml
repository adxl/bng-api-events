version: '3'

services:
  api:
    image: adxl/bng-api-events:latest
    build:
      context: .
      target: production

  db:
    image: adxl/bng-db-events:latest
    build:
      context: .
      dockerfile: Dockerfile.db
